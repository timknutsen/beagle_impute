name: alphaimpute2_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10        # 3.12 and 3.14 are explicitly incompatible with AlphaImpute2
  - pip
  - numpy>=1.19
  - numba>=0.49.0
  - pip:
    - AlphaImpute2
